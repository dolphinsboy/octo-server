name: Stale

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions: {}

jobs:
  stale:
    uses: Mininglamp-OSS/.github/.github/workflows/reusable-stale.yml@main
    permissions:
      issues: write
      pull-requests: write
